-c base.txt

# Dependencies only relevant for (unit) testing
black
coverage
django-jenkins
django-webtest
factory-boy
freezegun
flake8
pylint
pyquery  # integrates with webtest
requests-mock
tblib
pytest  # documentation tests
testfixtures
vcrpy
pytest-playwright 
docker
# XlsxWriter is more suitable for writing large files, but doesn't support reading them. 
# So for the tests we use openpyxl to check the created excel files.
openpyxl 

# For printing results of performance tests
tabulate

# Documentation
sphinx
sphinx_rtd_theme
